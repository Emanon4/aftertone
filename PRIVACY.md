# 隐私与数据说明

适用于当前开源实现，核查日期：2026-09-23。公开代码、浏览器数据、服务端任务和第三方服务分别处理如下。

## 密钥放在哪里

- 站点 Jev 密钥部署在 Cloudflare Worker secret 中，不打包进网页，也不提交到 Git。
- 用户填写的个人 API Key 保留在当前标签页的内存中。刷新、关闭页面或切回站点模式后清除；不写入 localStorage、sessionStorage、IndexedDB 或任务 JSON。
- 推荐请求通过 HTTPS 的 `X-Model-Api-Key` 请求头把个人 Key 交给本站 Worker，Worker 再调用所选模型服务。因此它会经过本站后端，并非只在浏览器内使用。
- D1 保存 Key 的 SHA-256 指纹，用于校验同一轮任务和区分每日额度，不保存 Key 原文。应用代码不打印密钥或上游原始错误内容。
- 默认模型地址受服务端白名单限制。增加自部署地址需要修改服务端配置，客户端不能任意指定转发目的地。

## 浏览器和服务端会保存什么

| 数据 | 位置与用途 |
| --- | --- |
| 收藏、标记为不合适的歌曲、深浅主题 | 当前浏览器 localStorage；不会作为账号资料同步到其他设备 |
| 起点歌曲、补充偏好、近期正负反馈、候选与评分结果 | D1 任务数据，用于分步筛选、恢复进度及避免重复调用 |
| 模型配置、Key 指纹、每日轮次 | D1，用于任务校验与额度管理 |
| 请求与运行信息 | GitHub Pages、Cloudflare 及第三方服务可能按自身政策处理访问日志和网络信息 |

每轮最多使用最近 8 条喜欢及 8 条不合适记录作为模型反馈。任务有效期为 30 分钟；当前清理逻辑在创建新任务时删除过期超过一天的任务及候选记录，因此不是固定时点的自动删除承诺。每日额度表目前没有自动清理机制，其中个人额度标识为 Key 指纹，不是 Key 原文。

任务使用随机 UUID。知道完整任务 ID 的人可以查询状态、结果并取消任务；推进个人模型任务还需匹配原 Key。当前没有用户账号和按账号隔离的私密任务空间，不应将任务 ID 作为公开分享链接。

## 哪些信息会发给第三方

- 搜索歌名、艺人以及解析试听，需要访问 Deezer 或 Apple/iTunes。
- 评分时，起点和候选歌曲资料、探索方向、补充偏好及近期反馈发送至当前选择的模型服务；不发送整段音频。
- 浏览器加载封面、播放试听或打开完整收听平台时，会连接对应服务。各服务可能接收 IP、浏览器信息等常规网络数据，并适用自己的隐私政策。
- 当前前端没有接入广告 SDK 或独立行为分析 SDK。这不代表托管平台和模型提供方完全不处理访问数据。

## 开源与检查范围

公开仓库包括代码、文档、音乐元数据索引及部署配置。数据库 ID、公开 API 地址、GitHub 用户名属于可见配置或公开标识，不是登录凭证。

2026-09-23 对源码仓库和 Pages 仓库当前文件、可达 Git 历史、已发布网页资产进行了已知密钥精确匹配及常见凭据模式检查，未发现密钥公开泄露。`.env`、`.dev.vars`、本地工具状态、日志和测试输出均被 Git 忽略。检查结果不等于对所有第三方系统或历史访问情况的审计。

Git 提交的作者名称和邮箱属于公开元数据，需另行检查；配置隐藏邮箱只影响未来提交，不会自动改变历史记录。项目后续提交使用 GitHub noreply 邮箱。
